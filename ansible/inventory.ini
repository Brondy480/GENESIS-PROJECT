# inventory.ini — List of servers Ansible will manage
# In production this would have your real server IP address

[webservers]
# Replace with your actual server IP when deploying to production
localhost ansible_connection=local

[webservers:vars]
ansible_user=ubuntu
ansible_python_interpreter=/usr/bin/python3
app_directory=/opt/genesis